package main
import "fmt"
const NMAX int = 100
type pinjaman struct {
  nama string
  pinjaman, tenor int
}
type tabPinjaman[NMAX] pinjaman
func main() {
  
}

func bacaData() {
	var i int
	fmt.Scan(n)
	for i = 0; i < *n; i++ {
		fmt.Scan(&A[i].nama, &A[i].pinjaman, &A[i].tenor)
	}

func cetakData() {
  
}
